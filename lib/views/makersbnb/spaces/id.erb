<!DOCTYPE html>
<html>
  <head>
  <title>A DIY BNB</title>
  <% if @user %>
    <h4> Logged in as <%= @user.email%></h4>
    <form action="/makersbnb/sessions/logout" method="POST">
      <input type="submit" value="Log out">
    </form>
  <% end %>
  <a href="/makersbnb/about" >About us</a>
  <br>
  <a href="/makersbnb/requests" >Requests</a>
  </head>
  <body>
  <h1><%= @space.name %> <%= @space.price %></h1>
  <br>
  <h3><%= @space.description %></h3>
  <br>
  <form action='/makersbnb/spaces/<%= @space.id %>' method='POST'>
    <input type='date' placeholder='Available From (dd-mm-yyyy)' value"" name='start_date' required>
    <input type='date' placeholder='Available To (dd-mm-yyyy)' value"" name='end_date' required>
    <input type='submit' value='Check availability'>
  </form>
  <% if @availability == true %>
    Space is available! Go ahead and make a request.
    <form action='/makersbnb/spaces/<%= @space.id %>' method='post'>
      <input type='submit' value='Request the Space'>
    </form>
  <% elsif @availability == false %>
    Sorry, the space is not available for these dates. Try another set of dates.
  <% else %>

  <% end %>
    </ul>
  </body>
</html>
